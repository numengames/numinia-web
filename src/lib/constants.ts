/**
 * Shared constants. Use these instead of hardcoding URLs.
 * SITE_URL reads from env at runtime for self-hosting support.
 */

export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL || 'https://numinia.store';
