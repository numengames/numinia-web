# @numinia/com — numinia.com

The **production track** ("is it excellent?"). Deliberately a skeleton until
Phase 2: one page proving the workspace builds, type-checks, and consumes the
shared tokens. Full coverage + e2e + Cucumber requirements apply the moment
real features land here. PRs required; no direct pushes (once the remote exists).
