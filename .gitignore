# Dependencies
node_modules/

# Build outputs
dist/
.astro/
.turbo/
.vercel/
coverage/
.stryker-tmp/

# Environment — never commit secrets (it has happened before)
.env
.env.*
!.env.example

# Editor / OS noise
.DS_Store
.idea/
.vscode/
*.log
stryker-setup-*.js
apps/store/test-results/

# Wrangler build artefacts (generated on every deploy build)
.wrangler/

# The real seminal manual, fetched from the private lore repo (npm run
# lore:fetch). Lore never re-enters this repository's git tree.
apps/store/.lore/

# Local wrangler dev secrets (never committed)
.dev.vars

# STD-023 §19 — generated by scripts/share-card.mjs at build
apps/store/public/favicon.svg
apps/store/public/favicon.png
apps/store/public/apple-touch-icon.png
apps/store/public/og-card.png
